#include <iostream>
#include <string.h>
#include <vector>
using namespace std;
#define I64 long long
#define I32 int
#define F0R(b) for(I64 i=0;i<b;i++)
#define STR string
const int MAX=2e5+10;
I32 main()
{
    I64 n,m,a[MAX],b[MAX];
    cin>>n>>m;
    F0R(n) cin>>a[i];
    F0R(m) cin>>b[i];
}